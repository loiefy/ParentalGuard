namespace ParentalGuard.UI.Services.IpcClient;

/// <summary>
/// Facade domain Dashboard status/health check (Architecture/10-ui-architecture.md mục 2.2/5,
/// `MISC-050`/`FE-041`) — dùng ở `S2`. CHƯA implement ở giai đoạn nền tảng này — điền ở giai đoạn
/// viết `S2` (`DashboardStatusQuery`/`AcknowledgePauseAnomalyRequest`).
/// </summary>
public interface IDashboardFacade;
