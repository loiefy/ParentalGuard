namespace ParentalGuard.UI.Services.IpcClient;

/// <summary>
/// Facade domain cấu hình (Architecture/10-ui-architecture.md mục 2.2/5, `FE-012`/`PERF-050b`) —
/// dùng ở `S4`. CHƯA implement ở giai đoạn nền tảng này — điền ở giai đoạn viết `S4`
/// (`ConfigQuery`/`ConfigUpdateRequest`/`RemoveWhitelistEntryRequest`).
/// </summary>
public interface IConfigFacade;
