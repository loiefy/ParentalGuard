# ParentalGuard.Vision

Placeholder — capture (Desktop Duplication API/DXGI) + AI inference (ONNX Runtime), single-purpose tuyệt đối, chạy trong session tương tác qua `CreateProcessAsUser` (`ADR-01`), quyền hạn chế, không network/registry/file write ngoài đọc model (`SEC-017`/`018`). Chưa có code. Chờ `Architecture/02-process-architecture.md`, `05-image-pipeline-architecture.md`, `06-security-architecture.md` được viết & approve trước khi scaffold `.csproj` thật.
