# ParentalGuard — Tài liệu Thiết kế Hệ thống (System Design / Architecture)

> **Giai đoạn**: `[2] Thiết kế hệ thống` (đã chuyển từ `[1] Viết Spec` — xem `../Specification/00-INDEX.md` mục 1)
> **Trạng thái**: `DRAFT` — đang viết, chưa review/approve
> **Cập nhật lần cuối**: 2026-09-24

---

## 1. Mục đích bộ tài liệu này

Bộ tài liệu này trả lời câu hỏi **"làm thế nào" (HOW)** để hiện thực hoá **"cái gì" (WHAT)** đã chốt trong `../Specification/`. Mỗi tài liệu ở đây phải truy vết được ngược về Requirement ID tương ứng trong spec (`GEN-`, `BE-`, `SEC-`, ...) — không phát minh thêm yêu cầu sản phẩm mới ở bước này; nếu trong lúc thiết kế phát hiện thiếu/mâu thuẫn với spec, phải quay lại sửa spec trước (theo đúng quy trình archive ở `CLAUDE.md`), không tự ý quyết định ở tài liệu kiến trúc.

Cấu trúc tài liệu đi từ **high-level → low-level**, từ mục tiêu tổng thể xuống chi tiết implement:

```
[01] Tổng quan kiến trúc          ← bức tranh toàn cảnh: component, tech stack, nguyên tắc xuyên suốt
        ↓
[02-10] Kiến trúc từng mảng       ← chi tiết hoá từng component/luồng (process, data, IPC, security, pipeline, overlay, anti-tamper, UI...)
        ↓
[11+]  Kiến trúc triển khai/vận hành ← deployment, CI/CD, release, dev workflow
```

## 2. Cấu trúc bộ tài liệu

| # | File | Nội dung | Trạng thái |
|---|---|---|---|
| 00 | `00-INDEX.md` | File này — mục lục, quy tắc, trạng thái tổng | Draft |
| 01 | `01-tong-quan-kien-truc.md` | System context, component architecture, tech stack, nguyên tắc thiết kế xuyên suốt, bảng quyết định kiến trúc (ADR) | **Approved** |
| 02 | `02-process-architecture.md` | Chi tiết từng process (Service/Vision/Overlay/UI/Watchdog): lifecycle, trách nhiệm, state machine | **Approved** |
| 03 | `03-ipc-communication.md` | Named Pipe contract cụ thể: message schema, thứ tự gọi, xử lý lỗi/timeout, HMAC signing | **Approved** |
| 04 | `04-data-architecture.md` | Schema `config.db` (SQLite), format audit log (hash-chain), layout file trên đĩa | **Approved** |
| 05 | `05-image-pipeline-architecture.md` | Class/component design pipeline capture→crop→resize→inference→decision, threading model | **Approved** |
| 06 | `06-security-architecture.md` | ACL cụ thể (thư mục/registry), token/session handling, AppContainer, WFP rule, DPAPI application | **Approved** |
| 07 | `07-overlay-architecture.md` | Vùng loại trừ nút đóng 3 lớp (`FE-016`), đa cửa sổ vi phạm/z-index/giới hạn 10+chế độ gộp+full-screen lock (`BE-084`–`089b`, `FE-016f`/`g`), multi-monitor phía Overlay/Service + UX icon trạng thái đầy đủ (`BE-080`–`083`, `FE-020`–`022`) | Draft |
| 08 | `08-password-authentication-architecture.md` | Argon2id (tham số/thư viện/PHC format), memory hygiene RAM (`PWD-050`/`051`), IPC Setup/Auth Modal/Đổi mật khẩu/Khôi phục (`PWD-001`–`035`), rate-limit thực thi cụ thể | **Approved** |
| 09 | `09-anti-tamper-architecture.md` | Dual watchdog implementation, Service Recovery Options, custom uninstaller flow | **Approved** |
| 10 | `10-ui-architecture.md` | WinUI 3 app structure (MVVM), navigation map, resource/đa ngôn ngữ | **Approved** |
| 11 | `11-deployment-release-architecture.md` | Installer (MSI/MSIX), SignPath CI signing pipeline, GitHub Release flow | Chưa viết |
| 12 | `12-dev-automation-architecture.md` | Claude Code Agent workflow (Dev/Test/Debug/Report), GitHub Actions CI pipeline | Chưa viết |

## 3. Nguyên tắc truy vết (Traceability)

- Tài liệu kiến trúc **không có Requirement ID riêng** — mọi quyết định thiết kế phải trích dẫn ngược ID gốc trong spec (ví dụ: "Module `Vision` implement `BE-023a`"). Điều này tránh 2 bộ ID song song gây rối.
- Với quyết định kiến trúc thuần kỹ thuật, không map trực tiếp về 1 requirement cụ thể (ví dụ: chọn ORM nào, đặt tên namespace ra sao) → ghi dưới dạng **ADR (Architecture Decision Record)** ngắn gọn trong bảng ở mỗi file, không cần ID.

## 4. Quy tắc bảo trì (khác với `Specification/`)

- Tài liệu kiến trúc **được kỳ vọng thay đổi thường xuyên hơn spec** trong quá trình code thực tế (khi implement mới lộ ra chi tiết chưa lường trước) — vì vậy **không bắt buộc archive vào `Outdated/`** trước mỗi lần sửa như bên `Specification/`. Mỗi file vẫn có version header + bảng Changelog ở cuối để theo dõi lịch sử, nhưng không tạo bản sao riêng.
- Nếu 1 thay đổi kiến trúc kéo theo phải **sửa lại chính spec** (WHAT thay đổi, không chỉ HOW) — bắt buộc quay lại `Specification/` và làm đúng quy trình archive ở đó trước, rồi mới cập nhật tài liệu kiến trúc theo sau.
- Version từng file theo semver như bên spec (`PATCH`/`MINOR`/`MAJOR`), nhưng không có "version bộ tài liệu tổng" bắt buộc — chỉ theo dõi trạng thái Draft/Review/Approved theo cột ở mục 2.

## 5. Quy trình review tài liệu này

Giống quy trình Feature Gate đã chốt ở `TEST-002`/`TEST-003` (`11-testing-qa-process.md`): mỗi file kiến trúc viết xong → bạn review → approve hoặc yêu cầu sửa → mới sang file tiếp theo. Không viết dồn toàn bộ 10 file cùng lúc để tránh sai lệch lớn phải làm lại nhiều.

## 6. Changelog

| Ngày | Thay đổi |
|---|---|
| 2026-09-24 | **Đóng 2 câu hỏi mở còn treo ở `10-ui-architecture.md` v0.1.0 mục 11**, sau khi `spec-maintainer` xử lý xong `Specification/`. (1) `PERF-050`→`PERF-050b` (`Specification/08-performance-cpu-spec.md` v0.7.0, ĐÃ CHỐT bởi chủ dự án — supersedes `PERF-050`/`PERF-050a` nay `DEPRECATED`: bỏ hẳn mức "Tiết kiệm pin", chỉ còn 2 mức "Cân bằng" (mặc định)/"Bảo vệ tối đa" — không chấp nhận đánh đổi giảm hiệu quả bảo vệ trẻ em). Viết thiết kế UI thật cho mục "Chế độ hiệu năng" ở `S4` (`10-ui-architecture.md` mục 6.4): `RadioButtons` 2 lựa chọn, đọc/ghi qua `ConfigQuery`/`ConfigUpdateRequest.performance_mode`, không gate `S5` (cosmetic/operational, ADR-125). (2) `MISC-030` — xác nhận đã xử lý câu chữ "domain/app" ở lượt trước, không cần amendment kiến trúc, chỉ xoá câu hỏi mở. Bump `10-ui-architecture.md` v0.1.0→**v0.2.0** (MINOR), trạng thái `Draft`→**`Approved`**. Amendment: `03-ipc-communication.md` v0.8.0→**v0.8.1** (PATCH, `enum PerformanceMode` + field `performance_mode` trong `ConfigResponse`/`ConfigUpdateRequest`, ADR-126), `04-data-architecture.md` v0.3.0→**v0.4.0** (MINOR, field `performance_mode` mới trong `monitoring_state` JSON, mặc định `"balanced"` khi vắng mặt; `capture_interval_baseline_ms` viết lại thành giá trị dẫn xuất cố định theo mode — `"balanced"`=2000ms/`"maximum_protection"`=1000ms, ADR-127, số liệu đề xuất ban đầu chờ benchmark `PERF-061`), `05-image-pipeline-architecture.md` v0.2.1→**v0.2.2** (PATCH, liên kết cơ chế `wakeEvent`/`ControlVisionCommand` đã có sẵn với 2 mức mới, không đổi code pipeline). Không phát sinh gap mới — đủ điều kiện giao `feature-dev` code Đợt 6 trọn vẹn (không còn phần nào bị loại trừ ở `S4`) |
| 2026-09-20 | **Đợt 6 (Dashboard UI hoàn chỉnh, `ROADMAP.md` mục 4) bắt đầu.** Viết xong `10-ui-architecture.md` (v0.1.0, Draft) — kiến trúc tổng thể `ParentalGuard.UI` (WinUI 3 unpackaged, MVVM `CommunityToolkit.Mvvm`), xác nhận lại cơ chế khởi động độc lập qua Start Menu/Desktop shortcut (đối chiếu code `StatusIconForm`/`StatusIconManager` — xác nhận KHÔNG có click-to-open, đúng `02` mục 2.5 đã chốt từ trước, không thêm hành vi mới), xác nhận độc lập hoàn toàn với `Overlay`(WinForms)/`Uninstaller`(6th exe, `09` ADR-91). Thiết kế mới `UiIpcClient` (khác biệt kiến trúc căn bản với `IpcChildClient` dùng cho `Vision`/`Overlay`: không bootstrap khoá qua handle kế thừa `CreateProcessAsUser`, dùng khung khoá 2 giai đoạn zero-key→session-key theo `03` ADR-82 đã có sẵn từ Đợt 3; request/response tuần tự, polling 5s cho Dashboard thay vì push — đúng nguyên tắc "kênh UI không heartbeat định kỳ" từ Đợt 0). Navigation map đầy đủ `S1`-`S9` (`S7`-`S9` xác nhận thuộc `Overlay`, ngoài phạm vi file này). Thiết kế chi tiết `S1` Onboarding, `S2` Dashboard (health check `MISC-050`, biểu đồ `FE-070`–`072`, banner `PAUSE-021`), `S3` Audit log (phân trang, gate xác thực theo `PWD-020`, đánh dấu false-positive `MISC-030`), `S4` Settings (đổi mật khẩu, quản lý whitelist app, thông điệp overlay tuỳ biến `FE-012`/`FE-012a` — loại trừ tường minh mục ngôn ngữ/ngưỡng nhạy cảm/gỡ cài đặt theo đúng `FE-063`/`BE-091`/`09` ADR-91; loại trừ mục "Chế độ hiệu năng" do `PERF-050` chưa `APPROVED`), `S5` Auth Modal tái dùng 1 component cho mọi hành động nhạy cảm, `S6` Recovery. Localization tái dùng nguyên pattern `.resx`+`ResourceManager` đã có ở `Overlay` (không dùng `.resw`). 11 ADR (114-124). **Phát hiện 2 gap kỹ thuật cần đóng ngay để `S2`/`S4`/`S3` hoạt động được** (đã tự xử lý trong cùng lượt vì thuần HOW, không đổi WHAT): (1) thông điệp overlay tuỳ biến (`FE-012`) chưa có kênh IPC nào mang xuống `Overlay`, và code Đợt 1 hardcode sai câu mặc định — thiết kế message mới `OverlayMessageUpdate` + ghi rõ hướng sửa bug cho `feature-dev`; (2) `MISC-030` (đánh dấu false-positive) cần biết tên process của sự kiện `ContentBlocked` nhưng `VisionInferenceResult` chưa từng mang field này — thêm field `process_name` (additive). Amendment cùng lượt: `02-process-architecture.md` v0.2.0→**v0.3.0** (MINOR, mục 3a.7 mới — đóng câu hỏi mở `PAUSE-021` sau khi chủ dự án đã APPROVED ngưỡng ở `Specification/07-pause-resume-spec.md` v0.2.2 nhưng file kiến trúc chưa kịp cập nhật cơ chế), `03-ipc-communication.md` v0.7.0→**v0.8.0** (MINOR, 10 message mới field 98-99+140-159 khối UI, field `process_name` ở `VisionInferenceResult`, message `OverlayMessageUpdate`, 3 ADR mới 111-113), `04-data-architecture.md` v0.2.4→**v0.3.0** (MINOR, 3 field JSON mới `user_whitelisted_process_names`/`overlay_message`/`anomaly_pending_ack`, định nghĩa lần đầu schema `ContentBlocked.detail`), `05-image-pipeline-architecture.md` v0.2.0→**v0.2.1** (PATCH, field `ProcessName`), `07-overlay-architecture.md` v0.2.2→**v0.2.3** (PATCH, mục 4.4 mới + ghi nhận bug Đợt 1, vẫn giữ `Draft`), `08-password-authentication-architecture.md` v0.3.0→**v0.3.1** (PATCH, hằng số `action_context="manage_whitelist"` mới). **2 câu hỏi mở CHÍNH cần `spec-maintainer`/chủ dự án xử lý trước khi implement trọn vẹn phần tương ứng**: (a) `PERF-050` (`Specification/08-performance-cpu-spec.md` mục 6) còn mang tag `(PROPOSED)` trong chính văn bản dù file đóng dấu "Approved" ở cấp toàn file — cùng dạng gap 2-tầng-trạng-thái đã gặp ở `PAUSE-021`/`ANTI-060` trước khi chốt, tự nêu rủi ro sản phẩm "tiết kiệm pin làm giảm hiệu quả bảo vệ" chưa được cân nhắc — **blocking** riêng mục "Chế độ hiệu năng" của `S4`, không blocking phần còn lại; (b) `MISC-030`/`S4` dùng chữ "whitelist domain/app" nhưng kiến trúc phát hiện hiện tại thuần pixel/window, không có cách nào biết "domain" (nhất quán triết lý gốc dự án ở `01-tong-quan-va-pham-vi.md`: "độc lập với domain/URL") — đề nghị sửa câu chữ spec, **không blocking** (phần "app" đã thiết kế đủ). Toàn bộ phần còn lại của Đợt 6 đã thiết kế đủ chi tiết để `feature-dev` implement thẳng. Theo chỉ đạo — không dừng chờ review từng file, dừng lại báo cáo sau khi xong đủ file mới + toàn bộ amendment |
| 2026-09-20 | **Đợt 5 (Pause/Resume, `ROADMAP.md` mục 4) bắt đầu.** Amendment `02-process-architecture.md` v0.1.4→**v0.2.0** (MINOR, mục 3a mới) — cụ thể hoá đầy đủ state machine Pause/Resume: kích hoạt qua `action_token` dùng chung `action_context="pause_monitoring"` cho cả Pause (`PAUSE-001`) lẫn resume sớm (`PAUSE-004`, ADR-101, đúng dự kiến sẵn ở `08` mục 7.2); tính `pause_expires_at_unix_ms` theo 5 lựa chọn thời lượng (`PAUSE-002`) dùng monotonic anchor "trusted_now" tái áp dụng từ `08` ADR-77 (ADR-105, chống bypass đổi giờ hệ thống); auto-resume + banner nhắc qua `PauseMonitor` tick 30 giây thay vì Timer bắn đúng hạn (ADR-102, tránh trôi lịch sleep/hibernate), khôi phục đúng sau restart `Service` giữa lúc Pause kể cả hết hạn lúc offline; cadence heartbeat `Service`↔`Vision` giảm còn 10s khi Paused kèm reset bộ đếm miss lúc chuyển trạng thái (ADR-104); giải phóng toàn bộ overlay đang che ngay lúc Pause kích hoạt (ADR-106, đúng dự đoán ở `ROADMAP.md` mục 2); xác nhận tường minh Pause hợp lệ tách biệt hoàn toàn khỏi bộ đếm `ANTI-060` (không sinh sự kiện nào bị đếm); xác nhận cơ chế suspend `Vision` đã được `05` giải quyết từ trước (đóng nốt câu hỏi mở còn sót ở mục 8). 6 ADR mới (101-106). Amendment cùng lượt: `03-ipc-communication.md` v0.6.0→**v0.7.0** (MINOR, 6 message mới field 92-97 khối UI — `PauseMonitoringRequest`/`Response`, `ResumeMonitoringRequest`/`Response`, `PauseStatusQuery`/`Response`, ADR-107), `04-data-architecture.md` v0.2.2→**v0.2.3** (PATCH, định nghĩa `detail` schema cho `PauseActivated`/`PauseResumed` — xác nhận schema `pause_state` đã đủ từ Đợt 0, không cần amendment), `07-overlay-architecture.md` v0.2.1→**v0.2.2** (PATCH, thiết kế banner `S9` tái dùng `ShowToastCommand` — ADR-108, đóng câu hỏi mở, vẫn giữ `Draft`). **1 câu hỏi mở CHÍNH cần `spec-maintainer`/chủ dự án xử lý trước khi implement riêng phần đó** (`PAUSE-021` — cảnh báo tần suất pause bất thường — còn mang tag "(PROPOSED)" trong chính văn bản `Specification/07-pause-resume-spec.md`, chưa từng được chuyển trạng thái `APPROVED`/`REJECTED` theo đúng workflow `Specification/00-INDEX.md` mục 1, dù file đó đã đóng dấu "Approved" ở cấp toàn file; ngưỡng ví dụ "> 5 lần/ngày" chưa `ĐÃ CHỐT` — không tự bịa số liệu cảnh báo bảo mật, cùng tinh thần đã áp dụng cho `ANTI-060` trước khi chủ dự án xác nhận N/T trực tiếp) — **không blocking phần còn lại của Đợt 5**, toàn bộ luồng Pause/Resume cốt lõi (kích hoạt/resume/auto-resume/banner/tương tác `ANTI-060`/`Vision`) đã thiết kế đủ chi tiết để `feature-dev` implement thẳng. Theo chỉ đạo — không dừng chờ review từng file, dừng lại báo cáo sau khi xong đủ 4 file (1 amendment chính + 3 amendment phụ) |
| 2026-09-20 | **Chủ dự án xác nhận/chốt câu hỏi mở chính duy nhất còn treo ở `09-anti-tamper-architecture.md` v0.1.0** (mục 10) — ngưỡng `ANTI-060`: **N=5 lần / T=30 phút** (lỏng hơn ví dụ minh hoạ gốc "5 lần/10 phút" trong `Specification/05-anti-uninstall-tamper-spec.md` mục 7, chưa từng `ĐÃ CHỐT`), áp dụng đồng nhất cho cả 2 bộ đếm RAM-only đã thiết kế (`Service`-side: Vision/Overlay/WFP/registry-tamper; `Watchdog`-side: số lần khôi phục `Service`). Bump `09` v0.1.0→**v0.1.1** (PATCH), trạng thái Draft→**Approved**. Không kéo theo sửa `Specification/` hay file `Architecture/` khác (thuần chọn số liệu HOW trong khoảng WHAT đã chốt). Theo chỉ đạo chủ dự án — không dừng chờ review, đủ điều kiện giao `feature-dev` code Đợt 4 |
| 2026-09-19 | **Đợt 4 (Anti-uninstall/Tamper protection, `ROADMAP.md` mục 4) bắt đầu.** Viết xong `09-anti-tamper-architecture.md` (v0.1.0, Draft) — đóng 2 câu hỏi mở treo từ Đợt 0 (giao thức `Watchdog↔Service`: Named Pipe riêng `ParentalGuard.Svc.Watchdog`, heartbeat 3s/3-miss). Dual Watchdog đầy đủ (`ANTI-010`/`011`): nguyên tắc tối giản `Watchdog` (không đụng `config.db`/`audit.log` trực tiếp, báo cáo qua IPC), trình tự khôi phục peer (stop→force-kill→start→re-register qua `sc.exe create` nếu registry mất), SCM Recovery Options tự cấu hình idempotent. Giám sát chủ động registry (`ANTI-031`) qua `RegNotifyChangeKeyValue` 2 chiều + self-heal. Custom Uninstaller đầy đủ (`ANTI-020`): 6th executable `ParentalGuard.Uninstaller.exe` (manifest `requireAdministrator`, không `QuietUninstallString`), pipe mới `ParentalGuard.Svc.Uninstaller` tái dùng `AuthVerifyRequest`/`action_token` từ `08`, luồng end-to-end 13 bước phân công `Service`(SYSTEM)/`Uninstaller`(Administrator), bất biến an toàn "không bao giờ tự xoá nếu Service không phản hồi SUCCESS", self-delete qua `MoveFileEx DELAY_UNTIL_REBOOT`. Thiết kế lần đầu bộ đếm `ANTI-060` (2 bộ đếm độc lập `Service`-side/`Watchdog`-side, RAM-only) + cơ chế "banner on-screen" tái dùng `IconState.ERROR` có sẵn (không schema UI mới). Xác nhận phạm vi `ANTI-070` không cần state fail-secure mới cho domain anti-tamper — chỉ ghi nhận minh bạch 1 rủi ro dư (khoảng trống bảo vệ ngắn khi `Service` bị kill, trước khi `Watchdog` khôi phục xong). 16 ADR (85-100). **1 câu hỏi mở CHÍNH cần chủ dự án quyết định trực tiếp** (ngưỡng N lần/T phút cho `ANTI-060` — spec chỉ đưa ví dụ minh hoạ, chưa `ĐÃ CHỐT`, không tự bịa số liệu bảo mật), 1 câu hỏi phụ không-blocking (mặc định checkbox giữ audit log lúc uninstall). Amendment cùng lượt: `02-process-architecture.md` v0.1.3→**v0.1.4** (PATCH, điền heartbeat Watchdog + đóng câu hỏi mở), `03-ipc-communication.md` v0.5.1→**v0.6.0** (MINOR, 2 pipe mới + field block 100-139 + `ProcessType.UNINSTALLER=6` + ràng buộc `Hello.process_type` theo pipe + sửa 3 field `recovery_key_plaintext`/`new_recovery_key_plaintext`×2 còn sót kiểu `string` từ trước khi `08` v0.3.0 đổi sang `bytes` — gap đồng bộ giữa 2 file, không phải quyết định mới), `04-data-architecture.md` v0.2.1→**v0.2.2** (MINOR, 4 `event_type` mới + mở rộng ngữ nghĩa 2 event đã có), `06-security-architecture.md` v0.1.2→**v0.1.3** (PATCH, 6th executable vào bảng ACL + sửa 1 tham chiếu số file "07" còn sót từ 2 lần renumbering trước), `07-overlay-architecture.md` v0.2.0→**v0.2.1** (PATCH, mở rộng mô tả `IconState.ERROR` cho banner `ANTI-060`, vẫn giữ Draft). Theo chỉ đạo — không dừng chờ review từng file, dừng lại báo cáo sau khi xong đủ file mới + toàn bộ amendment |
| 2026-09-20 | **Sửa 2 FAIL cứng (TEST-001 zero-tolerance) do `security-privacy-auditor` phát hiện lúc audit Đợt 3 (Password & Authentication)** — `08-password-authentication-architecture.md` v0.2.0→**v0.3.0** (MINOR). FAIL 1: `recovery_key_plaintext`/`new_recovery_key_plaintext` đổi từ `string` sang `bytes` trong `ipc.proto` (3 message: `SetInitialPasswordResponse`/`ChangePasswordResponse`/`RecoveryResetResponse`) — bỏ ngoại lệ `string` trước đó vi phạm ADR-73 mà không có phân tích rủi ro biện minh; thêm mục 5.5/ADR-83 định nghĩa thời điểm zero riêng cho field "hiển thị 1 lần" (zero ngay sau khi response đã ghi xong vào pipe, không phải sau khi hash xong) + bỏ field `recovery_key_plaintext` khỏi `AuthState.PendingSetup` (thu hẹp cửa sổ tồn tại từ tối đa 30 phút xuống mili-giây). FAIL 3: thêm mục 7.9/ADR-84 — cơ chế bù thời gian chống timing oracle cho nhánh `auth.dat` corrupt/chưa-setup (`AuthVerify`/`ChangePassword`/`RecoveryReset`), chạy 1 lần Argon2id thật trên dữ liệu mồi (tham số đọc động từ mục 4.2, không hardcode số đo, không dùng `Task.Delay`) thay vì trả response gần như tức thời như trước (ADR-81 mới chỉ đồng nhất nội dung, chưa đồng nhất thời gian). Cả 2 thuần HOW, không kéo theo sửa `Specification/`. FAIL 2 (thứ tự xử lý logic) không thuộc phạm vi Architecture — giao thẳng `feature-dev`. Theo chỉ đạo — không dừng chờ review |
| 2026-09-20 | **Đóng 2 gap Đợt 3 do `feature-dev` báo cáo lại** (`docs/dependency-map.md` mục "Khoảng trống đã biết — Đợt 3", 2/4 mục — 2 mục còn lại để nguyên, dành Đợt 6/9). (1) `08-password-authentication-architecture.md` v0.1.1→**v0.2.0** (MINOR) — thêm mục 7.8 + ADR-81: chính thức hoá hành vi khi `auth.dat` TỒN TẠI nhưng KHÔNG đọc/giải mã được (coi = "đã configured", mọi verify trả "sai" đồng nhất, không tự sửa/wipe/regenerate) — xác nhận đúng/an toàn theo nguyên tắc fail-secure `01` mục 5, đánh giá tường minh đây KHÔNG phải gap WHAT (tương đương kịch bản "mất cả mật khẩu lẫn Recovery Key" đã được `Specification/06-password-management-spec.md` mục 5 + `Specification/05-anti-uninstall-tamper-spec.md` `ANTI-020`/`040`/`041` chấp nhận đánh đổi từ trước), ghi nhận 1 residual risk chưa implement (audit log/Toast cho sự kiện corrupt — không bắt buộc). (2) `03-ipc-communication.md` v0.5.0→**v0.5.1** (PATCH) — mục 5.3 + ADR-82: cụ thể hoá bit-level "chữ ký rỗng" của khung `Hello`/`HelloAck` đầu tiên kênh `UI` = khoá HMAC hằng số 32-byte-zero biết trước cả 2 phía (không đổi ý nghĩa ADR-19, không đổi code). Cả 2 amendment thuần HOW, không kéo theo sửa `Specification/`. Theo chỉ đạo — không dừng chờ review từng file trong lượt này |
| 2026-09-20 | **Chủ dự án xác nhận/chốt cả 4 mục nêu ở mục 9 của `08-password-authentication-architecture.md` v0.1.0** — bump `08` v0.1.0→**v0.1.1**, trạng thái Draft→**Approved**. (1) ADR-72: đổi Argon2id chính thức sang phương án nhẹ hơn `m=32MiB (32768 KiB), t=2, p=2` (thay đề xuất khởi điểm `m=64MiB,t=3,p=2`) — ưu tiên trải nghiệm mượt trên máy yếu/cũ, vẫn trên sàn tối thiểu OWASP; (2) ADR-75: đổi Recovery Key sang **24 ký tự** (120-bit entropy, 15 byte CSPRNG, biên dưới `PWD-030`) thay 32 ký tự (160-bit) — cập nhật lại phép tính entropy + đoạn xác nhận đủ an toàn theo `PWD-030`; (3) ADR-76 (rate-limit dùng chung 1 bộ đếm mật khẩu+Recovery Key) và (4) ADR-78 (`action_token` 15 giây, dùng 1 lần, RAM-only) giữ nguyên đề xuất ban đầu, chỉ đóng dấu "ĐÃ CHỐT bởi chủ dự án 2026-09-20". Xoá toàn bộ checkbox câu hỏi mở ở mục 9 file đó. Không kéo theo sửa `Specification/` hay file `Architecture/` khác (thuần chọn số liệu HOW trong khoảng WHAT đã chốt). Theo chỉ đạo chủ dự án — không dừng chờ review, đủ điều kiện giao `feature-dev` code Đợt 3 |
| 2026-09-19 | **Đợt 3 (Password & Authentication, `ROADMAP.md` mục 4) bắt đầu.** Viết xong `08-password-authentication-architecture.md` (v0.1.0, Draft) — xác nhận `04-data-architecture.md`/`06-security-architecture.md` đã đủ schema/ACL cho domain này từ Đợt 0 (không cần amendment nội dung lưu trữ), tham số Argon2id cụ thể (thư viện `Konscious.Security.Cryptography.Argon2`, `m=64MiB/t=3/p=2` khởi điểm cần benchmark Đợt 3, PHC format), memory hygiene xuyên suốt `UI`↔`Service` (field IPC kiểu `bytes` thay `string`, pinned array + `CryptographicOperations.ZeroMemory` thay `SecureString`, ghi nhận minh bạch giới hạn dư của Protobuf `ByteString`), format Recovery Key (32 ký tự Crockford Base32, 160-bit entropy), toàn bộ luồng IPC Setup/Auth Modal (`action_token` gate ngắn hạn cho Đợt 4/5 tái dùng)/Đổi mật khẩu/Khôi phục (field 80-91 khối UI của `03`), rate-limit thực thi cụ thể (bộ đếm chung mật khẩu+Recovery Key, chống bypass đổi giờ hệ thống qua monotonic anchor — không cần field persist mới), 10 ADR (71-80), 4 mục đề nghị chủ dự án xác nhận (không blocking). **Quyết định tổ chức tài liệu** (thẩm quyền `architecture-writer`): nội dung đủ lớn/độc lập để tách file riêng (không nhồi vào `04`/`06`) — tạo file mới đánh số `08` (chèn ngay sau `07-overlay-architecture.md`, đúng thứ tự Đợt 3), dồn 4 file "chưa viết" (không có file thật trên đĩa): `08-anti-tamper-architecture.md`→`09`, `09-ui-architecture.md`→`10`, `10-deployment-release-architecture.md`→`11`, `11-dev-automation-architecture.md`→`12`. Amendment cùng lượt (renumbering + nội dung nhỏ): `02-process-architecture.md` v0.1.2→**v0.1.3** (PATCH, cập nhật con trỏ `AuthState` trỏ sang `08` + đổi số tham chiếu anti-tamper), `03-ipc-communication.md` v0.4.0→**v0.5.0** (MINOR, thêm 12 message Password/Auth field 80-91 vào khối UI 80-99 + sửa renumbering + sửa 1 comment tham chiếu sai còn sót từ trước), `04-data-architecture.md` v0.2.0→**v0.2.1** (PATCH, thêm 2 dòng `event_type`: `AuthBruteForceThresholdReached` mới + `VisionNetworkBlocked` còn treo từ `06` v0.1.0), `06-security-architecture.md` v0.1.1→**v0.1.2** (PATCH, renumbering anti-tamper `08`→`09` + đóng câu hỏi mở `VisionNetworkBlocked` đã treo). Theo chỉ đạo chủ dự án — không dừng chờ review từng file, dừng lại báo cáo sau khi xong đủ cả 5 file (1 mới + 4 amendment) |
| 2026-09-19 | **Cập nhật `07-overlay-architecture.md` v0.1.0 → v0.2.0** theo 2 quyết định sản phẩm mới chốt qua `spec-maintainer` (sau khi file này viết xong lượt đầu): (1) `Specification/02-backend-spec.md` thêm `BE-088a`/`BE-089a`/`BE-089b` (v0.13.0) và `Specification/03-frontend-ui-spec.md` thêm `FE-016f`/`FE-016g` (v0.9.0) — full-screen lock ở chế độ gộp nay là quyết định đã CHỐT chính thức (thay thế nội dung "flagged for review" của v0.1.0), bổ sung auto-timeout 30 giây làm lối thoát dự phòng thứ 2 (`GEN-007b`) + audit log phân biệt nguồn `"manual"`/`"auto-timeout"`; (2) mở rộng đầy đủ UX icon trạng thái multi-monitor (`FE-020`–`022`) vào Đợt 2 luôn (trước đó chỉ làm phần cấu trúc). Amendment cùng lượt: `03-ipc-communication.md` v0.3.0 → **v0.4.0** (field `source`/enum `CloseSource` trong `ForceCloseRequest`; 3 message mới `MonitoringStatusUpdate`/`IconPositionUpdate`/`IconLayoutSync` cho icon trạng thái/vị trí) và `04-data-architecture.md` v0.1.0 → **v0.2.0** (bổ sung event `ForceCloseRequested` + field `source` vào bảng event_type audit log; bảng mới `icon_positions`). Theo chỉ đạo chủ dự án — không dừng chờ review, đủ điều kiện giao `feature-dev` code Đợt 2 sau lượt này |
| 2026-09-19 | **Đợt 2 (Overlay hoàn chỉnh, `ROADMAP.md` mục 4) bắt đầu.** Viết xong `07-overlay-architecture.md` (v0.1.0, Draft) — vùng loại trừ nút đóng 3 lớp (`FE-016c`: fallback đồng bộ hiển thị ngay + UI Automation bất đồng bộ 150ms nâng cấp), đa cửa sổ vi phạm (xác nhận `BE-084`–`086` đã đúng cấu trúc từ code Đợt 1, thêm z-index cục bộ qua `SetWindowPos` lặp cho `BE-087`, giới hạn 10 + hysteresis 8 cho `BE-088`, chế độ gộp full-system handles cho `BE-089`, đình chỉ `FE-016` ở chế độ gộp — flagged rõ để chủ dự án review), multi-monitor phía Overlay/Service (icon cấu trúc + hot-plug `BE-083`, DPI awareness bắt buộc). Đồng thời amendment `05-image-pipeline-architecture.md` v0.1.0→**v0.2.0** (MINOR, thêm mục 3.5 multi-monitor/đa cửa sổ phía `Vision` — `BE-080`–`082`, `IMG-020`) và `03-ipc-communication.md` v0.2.0→**v0.3.0** (MINOR, thêm 2 field `is_merged`/`merged_window_handles` vào `OverlayRect` cho `BE-088`/`089`, additive không phá vỡ client cũ). **Quyết định tổ chức tài liệu** (thuộc thẩm quyền `architecture-writer`, không phải quyết định sản phẩm): `ROADMAP.md` mục 3 ghi Đợt 2 phụ thuộc file `02`/`03`/`08` — nhưng `08` đã được `00-INDEX.md` gán cho `08-ui-architecture.md` (Dashboard WinUI 3, Đợt 6, khác hoàn toàn Overlay WinForms đã code ở Đợt 1) từ trước khi `ROADMAP.md` viết bảng đó — sai lệch câu chữ, không phải chỉ dẫn cứng. Vì nội dung Overlay hoàn chỉnh đủ lớn/khác biệt để xứng đáng 1 file riêng, tạo file mới đánh số `07` (chèn ngay sau `06`), dồn 4 file "chưa viết" (không có file thật trên đĩa, chỉ là placeholder trong bảng mục lục — xác nhận trước khi dồn): `07-anti-tamper-architecture.md`→`08`, `08-ui-architecture.md`→`09`, `09-deployment-release-architecture.md`→`10`, `10-dev-automation-architecture.md`→`11`. Cập nhật đồng bộ toàn bộ tham chiếu bằng số ở 3 file đã `Approved` bị ảnh hưởng: `02-process-architecture.md` v0.1.1→**v0.1.2** (PATCH), `06-security-architecture.md` v0.1.0→**v0.1.1** (PATCH) — cả 2 chỉ đổi số file tham chiếu, không đổi nội dung quyết định; `03-ipc-communication.md` gộp chung vào bump MINOR ở trên. Theo chỉ đạo chủ dự án — không dừng chờ review từng file trong lượt này, dừng lại báo cáo sau khi xong đủ cả 4 file (1 mới + 3 amendment) |
| 2026-09-18 | Amendment `02-process-architecture.md` v0.1.0 → **v0.1.1** (PATCH, giữ trạng thái Approved, không cần archive theo mục 4) — phát sinh từ code thật Đợt 1 (Image Processing Pipeline), `feature-dev` phát hiện lỗi thiết kế vật lý ở mục 2.4/5.1: bản cũ ghi "Overlay gửi window handle về `Service` để `Service` force-close" — bất khả thi vì `Service` chạy Session 0 (`BE-023a`), không có quyền gọi `user32` lên HWND thuộc session tương tác. Sửa lại: `Overlay` tự gọi `PostMessage(WM_CLOSE)` cục bộ, đồng thời vẫn gửi `ForceCloseRequest` lên `Service` để cập nhật state + audit log (`BE-032` không đổi, không đổi IPC schema `03-ipc-communication.md`). Đây là gap HOW thuần kỹ thuật, không đổi WHAT nên không cần sửa `Specification/`. Theo chỉ đạo chủ dự án — không dừng chờ review |
| 2026-09-17 | Amendment `03-ipc-communication.md` v0.1.0 → **v0.2.0** (MINOR, giữ trạng thái Approved, không cần archive theo mục 4) — phát sinh từ code thật Đợt 0, `feature-dev` báo cáo lại 2 gap lúc implement `FailSecureConfigLoader`: (1) thêm message `ShowToastCommand`/enum `ToastSeverity` (field 62, khối domain Overlay) lấp thiếu message type Service→Overlay hiển thị Toast cho `BE-061b`; (2) thêm case `SERVICE = 5` vào enum `ProcessType` (trước đó code tạm lách bằng `PROCESS_TYPE_UNSPECIFIED`). Không đổi kiến trúc lớn, không cần sửa `Specification/`. Theo chỉ đạo chủ dự án — không dừng chờ review, `feature-dev` code tiếp ngay theo bản mới |
| 2026-09-17 | Chủ dự án approve `03`/`04`/`06`. Đợt 0 đủ điều kiện — chuyển `feature-dev` scaffold code thật |
| 2026-09-17 | Viết xong `06-security-architecture.md` (v0.1.0) — Đợt 0 hoàn tất đủ 4 file phụ thuộc (`02`/`03`/`04`/`06`). Token/Integrity Level pipeline cho `Vision`/`Overlay` (Low IL + Restricted Token cổ điển, không AppContainer — đóng câu hỏi mở `SEC-010` mục 10 `Specification/04-security-spec.md`), hệ quả Mandatory Label bắt buộc lên Named Pipe (bổ sung kỹ thuật cho `03`), WFP rule chặn network `Vision` 2 chiều (ALE layer, Provider/Sublayer riêng, persistent, áp dụng lúc `Service` Starting, idempotent) + phát hiện qua Security Event 5157 + xử lý kill-restart tái dùng `ANTI-060`, bảng ACL đầy đủ `%ProgramFiles%`/`%ProgramData%`/registry Service key, DPAPI application cụ thể (classic DPAPI machine-scope + entropy hằng số, không DPAPI-NG), 8 ADR (30-37). 3 câu hỏi mở nhỏ (validate DXGI/Low IL thực nghiệm ở Đợt 1, tinh chỉnh số liệu WFP, bổ sung 1 dòng `event_type` vào `04` ở lượt sửa kế tiếp). Theo chỉ đạo chủ dự án, không dừng chờ review — feature-dev bắt đầu scaffold code thật Đợt 0 |
| 2026-09-17 | Viết xong `04-data-architecture.md` (v0.1.0) — layout file đầy đủ trên đĩa (ACL chỉ SYSTEM cho `%ProgramData%`), schema `config.db` (5 bảng: `schema_meta`/`monitoring_state`/`pause_state`/`ipc_keys`/`audit_meta`, mã hoá DPAPI theo blob JSON/dòng), `auth.dat` tách biệt cho password/recovery key hash (`PWD-013`), format `audit.log` hash-chain JSONL (`MISC-010`/`SEC-041`) + xử lý chain đứt, luồng fail-secure chi tiết `BE-061`/`061a`/`061b`/`ANTI-070`, schema versioning, 7 ADR. Phát hiện 1 điểm không nhất quán nhỏ giữa `02-backend-spec.md` (bảng lưu trữ) và `SEC-041` về việc audit log có mã hoá DPAPI hay không — đã chọn theo `SEC-041` (không mã hoá nội dung), ghi vào câu hỏi mở đề nghị `spec-maintainer` sửa câu chữ `02-backend-spec.md` cho khớp. Theo chỉ đạo chủ dự án, không dừng chờ review — viết tiếp file kế tiếp |
| 2026-09-17 | Viết xong `03-ipc-communication.md` (v0.1.0) — 3 Named Pipe (Vision/Overlay/UI), ACL theo SID + xác thực chữ ký code-signing lúc connect (`BE-050`/`SEC-011`), framing length-prefixed + HMAC-SHA256 (`BE-051`/`SEC-012`), envelope Protobuf mở rộng được, message schema Đợt 0 + Đợt 1, bootstrap khoá HMAC qua anonymous pipe cho Vision/Overlay, session key ephemeral cho UI. 2 câu hỏi mở nhỏ (Watchdog protocol để ở `07`, UI message type để ở Đợt 6). Chờ review |
| 2026-09-17 | Chủ dự án APPROVE `01` và `02`. Bắt đầu code Đợt 0 (skeleton) song song viết tiếp `03`/`04`/`06` |
| 2026-09-17 | Viết xong `02-process-architecture.md` (v0.1.0) — lifecycle 5 process, state machine trung tâm `Service`, heartbeat, nguyên tắc đảm bảo mở rộng linh hoạt (Overlay declarative/Vision stateless/Service domain-state), trình tự khởi động boot. 2 câu hỏi mở nhỏ để ở `05`/`07`. Chờ review |
| 2026-09-17 | Khởi tạo bộ tài liệu Architecture, viết xong `01-tong-quan-kien-truc.md`, còn lại chờ review tuần tự |
